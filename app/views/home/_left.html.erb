<div class="flex flex-col p-4">
  <div class="flex flex-1 flex-col">
    <!-- menu items here -->
    <i class="fa fa-twitter fa-2x"></i>

    <div class="mt-5">
      <i class="fa fa-home fa-2x"></i>
      <span class="font-bold text-xl pl-3">Home</span>
    </div>

  </div>

  <div class="flex flex-none py-3">
    <%= image_tag current_user.avatar_name, class: 'rounded-full w-12 h-12 object-cover' %>
    <div class="ml-3 hidden md:block">
      <div class="font-semibold"><%= current_user.name %></div>
      <div class="text-gray-500">@<%= current_user.uid %></div>
    </div>
  </div>
</div>
